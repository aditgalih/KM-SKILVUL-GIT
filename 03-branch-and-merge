01. What does git clean do?
      for deleting untracked files in a repository's working directory.

02. What do the -d and -f flags for git clean do?
      -d is for deleting untracked directories, and -f is for deleting untracked files

03. What git command creates a branch?
      git branch <branch-name>

04. What is the difference between a fast-forward and recursive merge?
      fast-forward: if the merged branches have linear history (integrate histories)
      recursive: if the merged branches have different parent branches

05. What git command changes to another branch?
      git checkout <branch-name>

06. How do you remove modified or deleted files from the working directory?
      git checkout HEAD <file-name>

07. What git command deletes a branch?
      git branch -D <branch-name>

08. What does the git diff command do?
      show the detail of files changes in the working directory

09. How do you remove files from the staging area?
      git reset HEAD <file-name>

10. How do merge conflicts happen?
      if there are 2 or more identical files in different branches, and we merge those branches
